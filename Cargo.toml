[workspace]
members = ["crates/gbf-core", "src-tauri"]
resolver = "2"

[profile.release]
lto = "thin"
strip = true

# Keep compiler-loaded procedural macro metadata; strip the shipped executable only.
[profile.release.build-override]
strip = "none"
