# 安全政策

## 支援狀態

0.4.0 包含客戶端、Control、SSH Gateway 及自建部署工具。平台、協定與部署的已驗證範圍見[驗證紀錄](docs/validation.md)；未完成的原生或正式部署驗收分別列示。

## 信任邊界與安全要求

| 邊界 | 要求 |
| --- | --- |
| 靜態快取與動態通道 | 動態流量不進入本機 CA、內容解析、快取或重播流程 |
| 本機 CA | 私鑰僅留本機並受權限保護；具備明確的信任安裝及移除流程 |
| Control 與 Gateway | 獨立程序、服務帳號、資料及金鑰；管理接口只使用受權限限制的本機 socket |
| 客戶端服務配置 | 建置時僅內嵌部署識別、Control HTTPS URL 與公開憑證；授權碼、裝置私鑰與 host-key pin 按部署隔離 |
| Gateway 與目的地 | Ed25519 身份與短期租約、SSH host-key 驗證、目的地白名單及解析後的公開位址檢查 |
| 部署管理 | 已驗證 host key 的管理 SSH、主機身份核對與部署鎖；每主機一個 Gateway |
| 診斷與交付 | 排除認證資料、請求／回應內容、私鑰及真實部署秘密 |

詳細資料處理範圍見[隱私規格](docs/privacy.md)，部署要求見[部署規格](docs/self-hosted-gateway.md)。

## 漏洞回報

**私密漏洞回報管道尚未配置。** GitHub Private Vulnerability Reporting 列為公開前的配置項目，維護者須完成啟用與驗證後公布入口。

公開 issue、討論及提交不得包含未修復漏洞的利用細節、帳號資料、token、私鑰或實際部署設定。管道建立前，公開請求僅應說明需要私密回報方式，不附漏洞內容。

私密回報應包含受影響版本、影響範圍、隔離環境的重現步驟及已移除秘密的必要紀錄。安全驗證限於已授權的環境。

## 秘密與交付檢查

公開範例使用佔位值，真實設定及秘密與版本庫分離。`*.local.json`、加入 ticket、Control master key、TLS／SSH 私鑰及資料庫不隨產物分發。公開 Control 憑證可作建置輸入；它不具有私鑰或帳號授權能力。暫存內容須經檢查；`.gitignore` 僅作為輔助措施。

發現秘密暴露時，優先撤銷或輪換秘密，再評估受影響範圍並清理版本紀錄中的敏感內容。工作目錄中的刪除操作不會使已暴露的秘密失效。
